server:
  port: ":8080"
  host: "http://localhost"
  rootEndpoint: "/api"
  statusEndpoint: "/status"
  shortenEndpoint: "/shorten"
  findEndpoint: "/find"

database:
  type: "sqlite3"

shortUrl:
  length: 5
